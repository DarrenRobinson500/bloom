PromptBuild a full-stack calendar app with a Django REST API backend and a React frontend.The project must be structured for deployment to Railway. Review memories from the calendar project to make the transition to Railway smoother## Project Structure```bloom/ (this folder exists)├── backend/          # Django project│   ├── project/   # Django settings/urls│   ├── bloom_app/          # Django app│   ├── requirements.txt│   ├── Procfile│   └── manage.py├── frontend/         # React app (Vite)│   ├── src/│   ├── package.json│   └── vite.config.js└── README.md```## Backend — Django REST API### Setup- Django + djangorestframework + django-cors-headers + whitenoise + gunicorn + dj-database-url + psycopg2-binary- Settings configured for both local SQLite (development) and Railway PostgreSQL (production) via DATABASE_URL env var- CORS: allow all origins in development; restrict to RAILWAY_PUBLIC_DOMAIN in production- Serve the React build as static files via whitenoise from `backend/staticfiles/frontend/`### Models**user** - name - status - preferences - notes---## Frontend — React (Vite)### Dependencies- react-router-dom v6- axios- date-fns- Tailwind CSS### Environment- `.env.development`: `VITE\_API\_BASE=http://localhost:8000`- `.env.production`: `VITE\_API\_BASE=` (empty — same origin in production)### Routing — react-router-dom v6Wrap the app in `<BrowserRouter>` in `main.jsx`. Define these routes in `App.jsx`:### Components**App.jsx**- Defines the `<Routes>` tree- Renders a persistent top nav with links to `/calendar` and `/todos`- Hosts the shared `<EventModal>` and `<TodoModal>` state so modals can be triggered from any view**Landing Page** - Strawman version of the landing page```The dev server proxy means the frontend can call `/api/...` without CORS issues during local development.---## Railway Deployment### railway.toml (in project root)```toml\[build]builder = "nixpacks"buildCommand = "cd frontend \&\& npm install \&\& npm run build \&\& cd ../backend \&\& pip install -r requirements.txt"\[deploy]startCommand = "cd backend \&\& python manage.py migrate \&\& python manage.py collectstatic --noinput \&\& gunicorn calproject.wsgi --bind 0.0.0.0:$PORT"healthcheckPath = "/api/todos/"```### Environment variables to set in Railway dashboard- `SECRET\_KEY` — generate a secure random string- `DEBUG` — set to `False`- `DATABASE\_URL` — auto-injected by Railway when you add a Postgres plugin- `ALLOWED\_HOSTS` — set to your Railway domain e.g. `yourcalapp.up.railway.app`---## Final OutputAfter building, print:1. Local dev instructions: run backend with `python manage.py runserver`, run frontend dev server with `npm run dev`, seed data with `python manage.py seed`2. How to build the frontend for local Django serving: `npm run build` from the `frontend/` directory3. Railway deployment steps: create project, connect GitHub repo, add Postgres plugin, set the 4 env vars above, deploy